# homebrew-skiff

Homebrew tap for [skiff](https://github.com/johnlam90/skiff), the
mouse-first terminal code editor.

```sh
brew install johnlam90/skiff/skiff
```

The formula here mirrors `Formula/skiff.rb` in the main repo, where
each release's CI writes it; a sync workflow keeps this tap current.
